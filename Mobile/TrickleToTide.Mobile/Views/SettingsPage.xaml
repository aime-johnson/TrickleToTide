<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TrickleToTide.Mobile.Views.SettingsPage">
    <ContentPage.Content>
        <TableView Intent="Settings" VerticalOptions="Start">
            <TableSection Title="Connections">
                <TextCell Text="GPS Connected" Detail="{Binding GpsConnected}"/>
                <TextCell Text="Connection Status" Detail="{Binding ConnectionStatus}"/>

                <ViewCell>
                    <Button Text="Connect GPS" Command="{Binding ConnectCommand}"/>
                </ViewCell>
            </TableSection>


            <TableSection Title="User Prefs">
                <EntryCell Label="Name" Text="{Binding Nickname}"/>
            </TableSection>
        </TableView>
    </ContentPage.Content>
</ContentPage>